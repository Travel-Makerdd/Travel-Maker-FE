export interface FavoriteTrip {
  tripFavoriteId: number; // Unique identifier for the favorite trip
  tripId: number;         // Identifier for the trip
  tripTitle: string;      // Title of the trip
  tripDescription: string; // Description of the trip
} 